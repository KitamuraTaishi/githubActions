name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: bash ls command
        run: ls -la
      
      - name: clone repo
        uses: actions/checkout@v3
      
      - name: bash ls commmand again
        run: ls -la