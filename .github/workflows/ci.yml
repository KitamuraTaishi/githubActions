name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: aaaaaa
        run : printenv | grep 'TEST_'
        env: 
          TEST_CONTAINS: ${{ contains('TEST_', 'T) }}