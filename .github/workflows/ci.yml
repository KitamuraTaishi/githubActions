name: some Tests
run-name: Run by @${{ github.actor }}
on: push
jobs:
  comment:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: check github head ref
        run: echo "GTIHUB_HEAD_REF- ${GITHUB_HEAD_REF}"