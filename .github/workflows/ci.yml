name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: go version
        run: go version
      
      - name: setup go version
        uses: actions/setup-go@v2
        with:
          go-version: '1.22'

      - name: go version check again
        run: go version