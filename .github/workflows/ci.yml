name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: if check true
        run: echo "if true"
        if : ${{ true }}

      - name: if check false
        run: echo "if false"
        if : ${{ false }}